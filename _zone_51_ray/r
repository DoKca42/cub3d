if game_map[y][x] == 'N':
    draw_circle(x * MAP_RES + MAP_RES / 2, y * MAP_RES + MAP_RES / 2, 10, (82, 222, 62))
    player = {'x': x * MAP_RES + MAP_RES / 2, 'y': y * MAP_RES + MAP_RES / 2, 'ray_x': 0, 'ray_y': 0, 'direc': 75}